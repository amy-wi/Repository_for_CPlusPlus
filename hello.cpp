
//iostream is a header file that is part of c++ standard library. Here, it porvides std::cout and std::endl 

#include <iostream>

//All programs required "main()", it is the function executed when program starts.

//In this case, main()'s return type is int because 0 is returned when program runs correctly.

int main()

//Curly brackets start a "scope".
//std is the package from which these commands come from.
//cout means c-out, and prints output to screen.
//endl means end line.

{
    std::cout << "Hello from C++" << std::endl;

    return 0;
}

//Note, every line of code ends with a semicolon.
